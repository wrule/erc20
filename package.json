{
  "name": "erc20",
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "build": "rimraf build && truffle compile",
    "test": "truffle test",
    "migrate": "rimraf build && truffle migrate"
  },
  "repository": "https://github.com/wrule/erc20.git",
  "license": "MIT",
  "private": false,
  "dependencies": {
    "@openzeppelin/contracts": "^4.4.2",
    "@truffle/hdwallet-provider": "^2.0.0",
    "rimraf": "^3.0.2"
  }
}
